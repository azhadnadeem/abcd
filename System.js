class System{

    constructor(){}

    authenticate(bu=createSprite(500,330,30,30)){
       // actualCode,enteredCode
       // textSize(50);
       // fill("black");
       // text(code,300,300)
        if((bu=createSprite(500,330,30,30)) )
       // (actualCode === enteredCode.toUpperCase
           return false
        else
            return true
    }



    authenticate(fj=(20,20)){
        // actualCode,enteredCode
         textSize(50);
         fill("black");
         text(code,300,300)
         if((fj=(20,20)) )
        // (actualCode === enteredCode.toUpperCase
             return true
         else
             return false
     }



    




}
